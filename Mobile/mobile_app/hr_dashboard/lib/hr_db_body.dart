import 'package:flutter/material.dart';

class HRDBBody extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    // you should use the rectangle page tile class here
    // for testing just use in sized box or a container.
    // use logo_placeholder.png asset if assets are not availble. 
    return new Container();
  }

}